import PropTypes from 'prop-types';

export const coordinatesPropTypes = {
  accuracy: PropTypes.number,
  latitude: PropTypes.number,
  longitude: PropTypes.number,
};

export default {
  accuracy: null,
  latitude: null,
  longitude: null,
};
